<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# IR-048: Outcome authority and effect reconciliation

## Status

In progress

## Intent

Implement [DR-040](../decisions/040-outcome-authority-effect-reconciliation.md) so CODE, REVIEW, and DECIDE select and report outcomes from schema-validated semantics, durable repository evidence, and accepted runtime settlement rather than player-answer formatting.
Consumption and packed acceptance of the upstream transport repair gate only tasks 18 and 19; tasks 1 through 17 may proceed while that Cligent release is unavailable.

## Deliverables

- [x] Versioned authority metadata separates presentation, semantic, effect, and runtime-owned delegated-player outcome fields and declares each governed outcome's exact repository disposition, including a restricted deferred Boss-question continuation.
- [x] Plain-JSON configured workflow options stay disjoint from live current-host construction capabilities across fresh, restored, and adopted runtimes.
- [x] Repository observation and cooperative Playbook-writer coordination produce durable, fail-closed effect receipts without claiming to exclude or identify every foreign writer.
- [x] Effect-possible unresolved work and every earlier effect boundary in its turn survive process boundaries and every recovery path without replaying a player.
- [x] An effect-authorized Boss question preserves one cumulative operation and may make exactly one authored question-and-answer continuation per valid Boss answer from the identical latest saved repository checkpoint.
- [x] Exact bounded semantic adjudication reconciles with effect evidence while CODE's intent identity and progress fields remain semantic.
- [x] CODE, REVIEW, and DECIDE use the repository dispositions and commit identities decided by [DR-040](../decisions/040-outcome-authority-effect-reconciliation.md).
- [x] Accepted-outcome evidence is the single source for governed transition status and saved-count metrics, while Captain controller settlements consume canonical structured settlement, terminal-result, and bounded unresolved-effect facts whenever an episode remains parked or is abandoned.
- [x] Explicit unresolved-effect abandonment uses its own host-level disposal result outside the public `terminal` variant, reports bounded host-owned receipt identity, clears prior retained work for the root, disposes the complete engagement, and reaches no authored FSM outcome or parent resumption.
- [ ] A published Cligent patch preserves complete-message boundaries, and Playbook's dependency floor, lockfile, and packaged integration exercise that published artifact with Codex-shaped commentary followed by a final response.
- [ ] Specs, authored and generated artifacts, deterministic and integration suites, release gates, changelog, and candidate metadata agree and pass.

## Tasks

1. **Add schema-3 authority contracts.** _[done]_
   One commit: extend the SLC, linker, shared engine, registry, and CLI artifact contracts to accept artifact schema `3` alongside legacy schema `2` under runtime ABI `1`; validate exact field authority and per-outcome `unchanged`, `one-descendant-commit`, or restricted `deferred` repository dispositions or an explicit empty governed set; define disjoint plain-JSON configured-option and live host-construction-capability inputs with capability keys rejected from configuration and excluded from persisted identity; and land the corresponding `playbook`, `playbook-runtime`, `playbook-captain`, `playbook-cli`, and `release` items plus conformance tests without migrating workflow behavior.
2. **Build repository observation and cooperative leasing.** _[done]_
   One commit: add the detached HEAD and repository-relevant projection observer plus canonical-worktree coordination, prove exact `unchanged` and `one-descendant-commit` receipts across staged, tracked, non-ignored-untracked, ignored-only, unchanged pre-existing, residual, and ambiguous-overlap cases, serialize same-worktree governed calls except one dynamic declared zero-only cohort such as DECIDE's proposal pair, keep different worktrees independent, and land the corresponding runtime and CLI items plus focused tests.
3. **Inject the repository capability.** _[done]_
   One commit: make each current lease-owning host assemble artifact-specific observation, cooperative-coordination, atomic-ledger write-ahead, and canonical-worktree capabilities after configured-option and working-directory compatibility; pass them separately from configured options through every fresh, restored, and adopted schema-3 runtime-construction path without widening `PlaybookPorts`; reject absent or mismatched capability authority; prove no capability member reaches configuration, FSM context, snapshots, launch projections, or continuation equality; and keep the runtime, Captain, and CLI specs aligned with construction-path integration tests.
4. **Persist unresolved reconciliation.** _[done]_
   One commit: extend DR-031's leased uncertain record to atomically persist the ordered per-turn ledger of started physical boundaries, cross-turn deferred logical-operation identities, original baselines, checkpoints, checkpoint-restoration eligibility, logical and physical receipts, complete effect envelopes, and correction budgets during active work while safe runtime and shell snapshots carry a validated consistent mirror across teardown, restore, and ordinary continuation; persist only ledger data and canonical identities, never capability functions or live lease or store handles; reconstruct incomplete boundaries before source-state restoration; advance the decided runtime, shell, and durable-record schemas with explicit migration or pre-effect rejection; and land the `playbook-runtime`, `playbook-captain`, and `playbook-cli` items plus snapshot and cross-process tests.
5. **Fence runtime-local automatic replay.** _[done]_
   One commit: make empty-`ok` player correction and failure-state retry in the shared and DECIDE runtimes conditional on a complete durable `unchanged` receipt, and land the `playbook-runtime` and `playbook` items plus focused tests.
6. **Bind deferred Boss continuations.** _[done]_
   One commit: implement the effect-authorized `needsBossReply` path as one deferred logical operation whose host durably binds the pending question, player continuation, original baseline, and latest checkpoint before publication; on each later Boss turn reacquire the same-worktree claim, require exact latest-checkpoint equality, and start exactly one authored question-and-answer continuation per valid answer; retain physical checkpoint receipts while the final semantic arm uses one cumulative receipt from the original baseline; preserve that baseline across repeated questions; leave an invalid answer waiting without a call; park a mismatched checkpoint or other exit without a call; for a valid-answer checkpoint mismatch durably mark restoration eligibility, preserve the exact bound wait, and never reuse that answer; on an explicit reconciliation retry under a reacquired claim consume that eligibility to restore and republish the wait without a player or judge only when the saved checkpoint is exact, and leave every other retry unresolved; and land the `playbook-runtime`, `playbook-captain`, `playbook-cli`, and `playbook` items plus focused cross-turn integration tests.
7. **Fence uncertain whole-turn retry.** _[done]_
   One commit: reconcile every started boundary since the restorable pre-turn snapshot, permit CLI whole-turn replay only when every complete receipt proves `unchanged`, park nonzero, ambiguous, or incomplete boundaries, and land the `playbook-cli` items plus cross-process recovery tests.
8. **Fence retained-generation adoption.** _[done]_
   One commit: preserve effect ledgers and their validated snapshot mirrors with retained generations, make adopted runtimes reenter unresolved reconciliation, require outstanding boundaries to reconcile before adopted work resumes or exposes ordinary actions, prevent a retained pre-effect generation from bypassing an effect-possible boundary, and land the `playbook-runtime`, `playbook-captain`, and `playbook-cli` items plus adoption tests.
9. **Centralize semantic reconciliation.** _[done]_
   One commit: centralize exact declared-outcome validation, reject missing required, extra, wrongly owned, or inconsistent fields before FSM delivery, admit `deferred` only for an eligible effect-authorized question with a complete HEAD-stable checkpoint, await the host channel's durable one-call correction-budget spend before starting a corrective judge with a live abort signal, retain opaque presentation and semantic evidence without parsing repository facts, reconcile complete evidence or park unresolved, and land the linker and runtime contracts plus malformed, ownership, transport, abort, restart, and missing-evidence tests.
10. **Expose unresolved-effect controls.** _[done]_
   One commit: add the public `{ outcome: 'unresolved-effect', state }` run-result variant across the runtime, SLC, declarations, release contract, and exhaustive runtime consumers; advertise only reconciliation retry and abandonment while effect evidence remains unresolved; return an exactly restored open deferred operation to its bound wait through reconciliation without treating it as a final outcome or starting a player; keep bounded effect evidence out of that runtime-owned result; and cover the absence of a final state, `stateDescription`, output, nested-result translation, parent resumption, or completion claim with focused control tests.
11. **Settle host unresolved effects.** _[done]_
    One commit: add the required ordered bounded `unresolvedEffects` list to `PlaybookCaptainSettlement` and Captain session-record schema `5`; derive and validate it from every outstanding effect-possible, outcome-unresolved envelope, freeze it before each controller result-phase presentation and reuse that copy at the turn's later safe settlement, carry the current list at a safe settlement without controller work, keep it nonempty for a parked unresolved episode and abandonment but empty otherwise, and preserve each settlement's exact copy across restart; require exact HEAD and commit-OID presence invariants; durably mark abandonment started, dispose the complete root stack, return controller `ok` and `executed` only after atomically persisting the final evidence with the existing per-root `clear` update before Boss presentation, keep disposal or persistence failure `failed`, recover a crash by completing the settlement and clear without advertising an older generation, update Captain, CLI, and release declarations and contracts, and cover same-process, restored, and adopted parking plus root and nested-leaf abandonment with integration tests.
12. **Stage accepted-outcome consumers.** _[done]_
    One commit: teach the shared and DECIDE runtimes to consume schema-3 artifact markers, publish only markers executed by XState and confirmed by the corresponding public root snapshot, emit no accepted outcome for rejected-guard fallback, derive schema-3 status without private inspection fields, retain the schema-2 legacy status path, drain evidence before public settlement, advance trace events to schema `4`, and land the runtime items plus fallback tests.
13. **Stage canonical Captain consumers.** _[done]_
    One commit: give every schema-3 session-Captain controller `SettlementEvidence` an exact validated copy of the host's frozen `unresolvedEffects` list alongside structured settlement and terminal-result evidence rather than an aggregate transcript; for controller settlements that leave an episode parked or record abandonment, render observed versus merely possible effects before any later recovery action with exact available HEAD and proven commit identity but no paths or internal envelope data; derive schema-3 saved-count telemetry only from accepted outcomes; retain raw-judge parsing only as a schema-2 legacy path; move the compiled Captain artifact to schema `3` with its explicit empty governed set; and update the `playbook-captain` and `captain-playbook` contracts plus integration tests.
14. **Migrate CODE to effect authority.** _[done]_
    One commit: remove CODE's `Commit:` response instruction and parser, assign `one-descendant-commit` to every commit arm and restricted `deferred` to `needsBossReply`, source `latestCommit` only from the qualifying receipt, keep `irNumber`, `irTask`, and `moreTasks` versus `finalTask` semantic, attach stable accepted-outcome markers for the staged consumers, move CODE atomically to artifact schema `3`, update the authoritative and generated artifacts, and land the `playbook` items plus formatting, question-continuation, and effect-matrix tests.
15. **Migrate REVIEW to effect authority.** _[done]_
    One commit: make every Reviewer arm require `unchanged`; assign the Coder reconciliation call's `committed`, `rejectedAll`, and `needsBossReply` arms to `one-descendant-commit`, `unchanged`, and restricted `deferred`, respectively; route mismatched or ambiguous evidence to unresolved reconciliation without replaying Coder; attach stable accepted-outcome markers for the staged consumers; move REVIEW atomically to artifact schema `3`; update the authoritative and generated artifacts; and land the `playbook` items plus question-continuation and reconciliation-matrix tests.
16. **Migrate DECIDE to effect authority.** _[done]_
    One commit: remove DECIDE's `Commit:` response instruction and parser, require `unchanged` for every proposal arm, assign the merge call's `committed` and `needsBossReply` arms to `one-descendant-commit` and restricted `deferred`, derive `latestCommit` from the qualifying receipt, reuse shared semantic reconciliation in its parallel runtime, attach stable accepted-outcome markers for the staged consumers, preserve concurrent zero-only proposals while serializing the merge call, move DECIDE atomically to artifact schema `3`, update the authoritative and generated artifacts, and land the `playbook` items plus question-continuation and effect-matrix tests.
17. **Complete the artifact-schema cutover.** _[done]_
    One commit: prove every shipped artifact, registry, generated sibling, packed fixture, CLI launch projection, and durable artifact reference uses schema `3`, remove schema `2` from the engine, registries, and CLI validators, delete the schema-2-only raw status and judge-parser fallbacks, retain runtime ABI `1`, update the runtime, Captain, CLI, and release contracts, and cover rejection of legacy artifacts before governed work.
18. **Consume the published transport repair.**
    One commit: when the repaired Cligent release is public, raise the dependency floor and lock, amend the `release` package contract, and add a packed integration case whose Codex-shaped commentary and final response remain separate through the installed dependency.
19. **Gate and close the intent.**
    One commit: add the final packed and cross-process acceptance matrix through the published Cligent dependency, update public documentation, changelog, and candidate metadata, run Spex, deterministic, build, fidelity, packed, and conditional manual tmux UX gates, mark this intent complete, and stop without tagging, pushing, or publishing Playbook.

## Verification

- Missing, glued, fenced, quoted, duplicated, or misleading `Commit:` prose cannot change a transition when semantic and repository evidence are otherwise equal.
- Schema-3 artifact validation rejects missing, extra, unknown, or inconsistent field-authority and per-outcome repository-disposition declarations; CODE Coder, REVIEW Coder reconciliation, and DECIDE merge calls are effect-authorized with restricted `deferred` question arms, while every REVIEW Reviewer and DECIDE proposal arm declares `unchanged`.
- Schema-3 reconciliation rejects every missing required, extra, wrongly owned, or mutually inconsistent outcome field before FSM delivery; a semantic reply cannot supply effect-owned `latestCommit`, and only a consistent `one-descendant-commit` receipt supplies its OID.
- CODE and DECIDE accept only `one-descendant-commit`; REVIEW cannot choose `rejectedAll` without `unchanged` or `committed` without `one-descendant-commit`.
- Staged, tracked, non-ignored-untracked, ignored-only, unchanged pre-existing, call-created-and-committed, pre-existing-overlay-consumed, residual, multiple-commit, rewritten or non-descendant, and ambiguous-overlap cases prove the exact repository-relevant projection and receipt rules.
- Same-worktree governed calls serialize, DECIDE's one dynamic declared zero-only proposal cohort still overlaps without revealing either proposal early, same-worktree calls outside that cohort wait for its common-baseline receipts, different canonical worktrees remain independent, and a read-only call's concurrent delta or any post-commit residual fails closed without a post-hoc path filter pretending to attribute it.
- Configuration cannot supply a host capability; fresh, restored, and adopted schema-3 construction receives the current lease-owning host's capability only after configured-option and working-directory compatibility, while snapshots and launch projections stay plain JSON, compare only configured identity, and contain no callback, store, or lease handle.
- An effect-authorized Coder may ask Boss after a worktree-only delta; restart and later turns preserve its linked physical receipts, original logical-operation baseline, and exact checkpoint; only an identical checkpoint permits the authored answer continuation; its final arm uses the cumulative logical receipt rather than the last physical receipt; repeated questions still allow at most one cumulative commit; an invalid answer starts no player and preserves the wait; and a changed checkpoint, other exit, missing output, multiple commits, or rewritten or ambiguous evidence starts no player and parks unresolved.
- After a valid answer meets a mismatched deferred checkpoint, only that unresolved episode carries durable checkpoint-restoration eligibility and the answer never becomes continuation input; a fresh directive or other exit carries no such eligibility; same-process, restored, and adopted reconciliation retries at an unequal checkpoint make no call and remain unresolved, while exact saved-checkpoint restoration durably reenters and republishes the same bound question and stable id without a player, judge, or semantic-candidate delivery, so only a later valid answer may start the authored continuation.
- A real or possible effect followed by empty player output, failed adjudication, failure-state retry, uncertain-turn recovery, or retained resumption survives restart without another player call; every started boundary since a restorable snapshot must prove `unchanged` before whole-turn replay, complete retained evidence reconciles, and missing semantic evidence remains parked with only reconciliation retry or Boss abandonment.
- Boss abandonment returns `{ outcome: 'unresolved-effect', state }`, carries no `stateDescription`, output, or duplicated effect receipt, reaches no authored final state, disposes the complete stack, resumes no parent FSM, and claims no workflow completion.
- At every safe settlement, `PlaybookCaptainSettlement` and Captain session-record schema `5` carry exact validated copies of one canonical `unresolvedEffects` projection of each outstanding effect-possible, outcome-unresolved envelope in ledger order, represent a deferred chain once, and use no new repository observation for presentation.
- Before each controller result-phase presentation, the host freezes that canonical list and schema-3 `SettlementEvidence` receives its exact copy, while a safe turn without controller settlement fabricates neither that evidence nor result-phase facts.
- Controller settlements exported from same-process, restored, and adopted unresolved episodes that remain parked and unresolved-effect abandonment carry nonempty lists, an unequal reconciliation retry stays nonempty, and the settlement completing reconciliation or exact wait restoration carries an empty list when no other unresolved episode remains.
- Every list entry carries exact bounded classification and baseline, after-HEAD exactly when observed, and commit identity exactly for a proved one-descendant receipt; the list excludes paths, projection contents, internal identifiers, prose, semantic candidates, and budgets, and each nonempty controller settlement's same-turn Boss reply before any later recovery action distinguishes observed from merely possible effects.
- Abandonment freezes the final nonempty list, atomically clears every prior retained generation for the complete root before presentation, survives a crash during disposal, and returns controller `ok` and `executed` only after successful persistence and disposal while failures remain `failed`.
- `irNumber`, `irTask`, and `moreTasks` versus `finalTask` remain schema-validated semantic fields and are never inferred from an unspecified intent-progress marker.
- An adjudicator selects exactly one declared outcome after at most one corrective judge call; the host-owned write-ahead channel durably spends that boundary's correction budget before the call, a failed, indeterminate, or aborted spend starts no call, transport failure, non-`ok`, or missing presentation evidence triggers none, and restart cannot restore a spent budget.
- Status and saved-count telemetry consume only governed artifact markers confirmed by public root snapshots, emit no claimed outcome when stricter validation selects a fallback, and require no underscore-prefixed XState inspection field.
- Session-Captain control receives only canonical structured settlement, terminal-result, and bounded unresolved-effect facts for outcome authority, never an aggregate transcript.
- Artifact schema `3` coexists with legacy schema `2` under runtime ABI `1` only during migration; no artifact declares schema `3` before its governed behavior agrees, and the next-major candidate ships only schema-3 artifacts with schema `2` rejected.
- Packed acceptance installs the published Cligent dependency and passes Spex lint, deterministic suites, build and fidelity checks, release smoke, and the cross-process recovery gate.
