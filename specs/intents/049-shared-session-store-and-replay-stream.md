<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# IR-049: Shared session store and replay stream

## Status

Incomplete

## Intent

Implement [DR-042](../decisions/042-shared-session-store-and-replay-stream.md) so the interactive launcher, headless runner, and an external host share one directory for canonical Playbook manifests, host sidecars, and token-free replay streams without changing the settled lifecycle capabilities or frozen stream ABI.
The behavior and verification contracts already live in [[playbook-cli-4](../packages/playbook-cli.md#playbook-cli-4)], [[playbook-cli-23](../packages/playbook-cli.md#playbook-cli-23)], [[playbook-cli-46](../packages/playbook-cli.md#playbook-cli-46)], [[playbook-cli-73](../packages/playbook-cli.md#playbook-cli-73)] through [[playbook-cli-81](../packages/playbook-cli.md#playbook-cli-81)], [[release-18](../packages/release.md#release-18)], [[release-28](../packages/release.md#release-28)], and [[release-33](../packages/release.md#release-33)].
Planning found one remaining completeness task before implementation: the semver-stable facade is named by subject but its exact JavaScript identifiers, signatures, declaration exports, token-free session summary, stream result and status shapes, and package-owned opaque replay-payload types are not yet exact enough to reimplement under [[meta-34](../meta.md#meta-34)].
Cross-repository review established that the dependent host's content writer matches the semantic JSONL ABI, while its current privacy modes and reader require adoption in that repository rather than a compatibility relaxation here.

## Deliverables

- [ ] The package specs name the exact narrow facade, token-free session-summary boundary, and opaque replay-payload boundary without adding a capability beyond [DR-042](../decisions/042-shared-session-store-and-replay-stream.md).
- [ ] The internal store reads and writes the frozen version-1 replay stream, strips provider resume credentials, and keeps only a clean contiguous readable prefix.
- [ ] Replay appends require the session lease, resume after the complete retained prefix, checkpoint at settlement and release, and latch and stop without failing an agent turn.
- [ ] The published JavaScript and declaration facade exposes only the decided module, token-free session summary, store, and narrow lease surfaces over the same private implementation and validators that the front ends use through their richer internal leases.
- [ ] Both front ends tee every observed host record in order, add roles where the trace makes them known, and change neither presentation nor turn outcomes.
- [ ] The optional top-level `sessions` locator resolves before selection in both front ends and never enters a persisted projection.
- [ ] A checked-in cross-repository fixture, focused integration coverage, packed-consumer smoke, documentation, changelog, and release gates verify the complete cutover without a version bump or release tag.

## Tasks

1. **Name the frozen public and replay shapes exactly.**
   One commit: amend [[playbook-cli-73](../packages/playbook-cli.md#playbook-cli-73)] to name the exact JavaScript identifiers, call signatures, declaration export set, listing report, exact five-field session-summary type, incremental stream-read result carrying `lastReadableSeq`, `lastDurableSeq`, and `incomplete`, matching live stream-status type, and release result required by the already-decided module, store, and narrow lease capabilities; define the facade's replay payload through package-owned opaque JSON object/value types that neither import nor re-export Cligent's observed-record union; make [[playbook-cli-79](../packages/playbook-cli.md#playbook-cli-79)] and [[playbook-cli-80](../packages/playbook-cli.md#playbook-cli-80)] pin those exact shapes; and leave [[release-33](../packages/release.md#release-33)] as the citation-bound release contract rather than a duplicate source.
2. **Implement the strict replay codec and reader.**
   One commit: extend the private session store with recursive token sanitization, strict version-1 closed-envelope and contiguous-sequence validation, opaque JSON-object payload preservation without `type` gating, a `0700` real non-symlink directory and `0600` regular non-symlink stream boundary, and lease-free newline-prefix reads that mutate nothing and ignore every unrelated host sidecar; check in the shared `.records.jsonl` content fixture and focused store integration tests for semantic fixture equality independent of JSON member order and source-checkout mode, unknown payload-type preservation, strict version and envelope rejection, missing or non-object payload rejection, malformed sequence, recursive sanitization, unsafe filesystem boundaries, foreign-sidecar preservation, and byte-identical torn-prefix reading under [[playbook-cli-79](../packages/playbook-cli.md#playbook-cli-79)] and [[playbook-cli-80](../packages/playbook-cli.md#playbook-cli-80)]; stop and report rather than normalize any content divergence in filename, parsed envelope, contiguous sequence, newline termination, or sanitizer semantics; make the licensing checker recognize the no-comment `.jsonl` format under [[licensing-5](../packages/licensing.md#licensing-5)]; and update the cited behavior first if implementation reveals a genuine defect.
3. **Bind replay mutation to the lease.**
   One commit: add held-lease torn-tail repair, writer-assigned ordered appends, separate last-readable and last-durable sequence state seeded together from the complete prefix actually retained, first-publication directory synchronization before accepting the first append, batched content checkpoints after successful private record settlement and before normal release, live latch-and-stop status, complete-prefix latched reads carrying incompleteness and both boundaries, successor-prefix recovery, final status returned from release, replay-failure isolation that preserves successful settlement and otherwise valid release, and refusal without exact ownership; land focused store integration tests for empty and retained-prefix initialization, repair, ownership, normal and successor sequencing, append-only readable advancement, positive settlement and release checkpoint advancement, publication and checkpoint faults, live-only latch reporting, unsanitizable input, retained complete lines beyond the durable boundary, and suppression of every later write under [[playbook-cli-76](../packages/playbook-cli.md#playbook-cli-76)] and [[playbook-cli-80](../packages/playbook-cli.md#playbook-cli-80)]; and update the cited behavior first if implementation reveals a genuine defect.
4. **Publish the narrow facade over the shared implementation.**
   One commit: add the decided sibling hand-written JavaScript and self-contained declaration artifacts, runtime-narrow store and lease wrappers over the private implementation, internal listing that reports every skipped canonical manifest, full private record validation followed by exact detached token-free summary projection, and package `files` and `exports` entries; update both `PUBLIC_MODULE_EXPORTS` and `PUBLIC_DECLARATION_EXPORTS`; prove structurally that the facade and both front ends depend on one private store and validator definition, that the credential-bearing manifest plus every lifecycle, validator, effect-ledger, staging, publication, or retirement member is unreachable through the wrapper, and that a local strict consumer with `skipLibCheck: false` resolves the declaration without an undeclared private-module or Cligent observed-record dependency; retain the front ends' richer leases, add no opaque bridge or second acquisition path, and keep [[playbook-cli-73](../packages/playbook-cli.md#playbook-cli-73)], [[release-18](../packages/release.md#release-18)], [[release-21](../packages/release.md#release-21)], and [[release-33](../packages/release.md#release-33)] synchronized spec-first if needed.
5. **Resolve the sessions locator at bootstrap.**
   One commit: add the locator-only bootstrap operation to the host-neutral launch-config module, call it in the interactive launcher before provisional list, read, or selection and in the headless runner before store construction or selection, and reuse the existing managed-child `sessionsDir` descriptor to carry the resolved value unchanged; cover unset, tilde, rejected `~user`, absolute, dot-relative and bare-relative, ordered overlay, injected-store precedence, and unusable-directory cases; exclude the locator from host, structural, and execution projections; keep the prior default path and existing Captain-record, config, stdout, and stderr bytes unchanged when unset, with the required replay sidecar as the sole intentional addition; and keep [[playbook-cli-4](../packages/playbook-cli.md#playbook-cli-4)], [[playbook-cli-23](../packages/playbook-cli.md#playbook-cli-23)], [[playbook-cli-46](../packages/playbook-cli.md#playbook-cli-46)], [[playbook-cli-78](../packages/playbook-cli.md#playbook-cli-78)], and [[playbook-cli-81](../packages/playbook-cli.md#playbook-cli-81)] synchronized spec-first if needed.
6. **Tee both host record streams.**
   One commit: register one awaited fail-soft replay observer outside each presentation gate on the full private lease the front end already owns; fold `captain_telemetry` records with topic `playbook.trace` by exact trace `(sessionId, callId)` frames, deriving local envelope roles from unambiguous `payload.playerId` and `payload.roleId` associations without confusing `record.playerId` or `record.event.role` for that local role; prove complete headless and managed streams including hidden events, nested-session call-id reuse, a rejected overlapping same-player trace pair that preserves its outer frame, and two-turn contiguous sequencing; prove sanitizer, append, or checkpoint failure changes neither the agent turn nor buffered stdout and stderr while the latching store reports an incomplete durable prefix; and keep [[playbook-cli-74](../packages/playbook-cli.md#playbook-cli-74)] and [[playbook-cli-77](../packages/playbook-cli.md#playbook-cli-77)] through [[playbook-cli-80](../packages/playbook-cli.md#playbook-cli-80)] synchronized spec-first if needed.
7. **Prove the packed external consumer.**
   One commit: extend the packed release smoke with a tarball-only external TypeScript consumer that uses only the facade to list and read the exact token-free summary of a credential-bearing manifest, lease, append, observe readable-ahead-of-durable status, replay, receive equalized final release status, and reject an old schema, compiling with `skipLibCheck: false`; cover the facade artifacts in package and smoke gates; and keep [[release-18](../packages/release.md#release-18)], [[release-28](../packages/release.md#release-28)], and [[release-33](../packages/release.md#release-33)] synchronized spec-first if needed.
8. **Document, verify, and close the intent.**
   One commit: update public configuration and CLI documentation plus `[Unreleased]` changelog entries; audit the package specs and `specs/map.md`; after every input has settled, run each expensive final gate once, including `vitest run --exclude src/cligent-release-capabilities.test.ts` with exactly three failures in `src/slc-source-contract.test.ts`, two in `reference/sdlc/code.playbook/code.gears-fsm.test.ts`, one in `reference/sdlc/decide.playbook/decide.gears-fsm.test.ts`, and no others, the excluded capability suite separately, `pnpm smoke:release`, build, lint, licensing, links, and focused package tests; mark this record done; and stop without bumping the version, tagging, pushing, or publishing.

## Verification

- The facade's runtime and declaration export sets equal the exact names recorded in the package specs, listing and reading a credential-bearing canonical manifest return only the exact five-field detached token-free summary, and no raw manifest, internal lifecycle, validator, effect-ledger, staging, or retirement member is reachable through the published module, store, or lease.
- The checked-in fixture parses and rewrites to semantically equal `<sessionId>.records.jsonl` envelopes with version `1`, optional role, sequence numbers contiguous from `1`, and one line feed after every envelope, independent of JSON member order and source-checkout mode.
- A missing or unknown envelope version, missing or unknown envelope member, missing or non-object record payload, malformed completed line, duplicate, missing, or noncontiguous sequence, symlink, non-regular stream, wrong file mode, or non-private or symlinked sessions directory fails closed without changing an unrelated host sidecar, while an opaque object carrying an unknown record type round-trips unchanged.
- Recursive sanitization removes every `resumeToken` and string-valued `resume`, preserves `resume: false`, latches rather than writes a value that cannot become safe JSON, and exact `(sessionId, callId)` role folding enriches only unambiguously associated player records while surviving nested call-id reuse and a rejected overlapping same-player bracket.
- Empty and retained-prefix initialization seed readable and durable sequence values together, lease-free reading returns the complete newline-terminated prefix without changing one byte, held-lease reading synchronizes only a torn-tail repair, successful appends advance only readability, first publication synchronizes its directory, settlement and release checkpoints advance durability to readability, normal release returns that final status, a successor lease continues after the complete prefix actually retained, and append without ownership fails closed.
- After one injected append, publication-sync, settlement-checkpoint, or release-checkpoint failure, no later record reaches the file, the same live store returns every complete readable line and reports both sequence boundaries plus incompleteness without rolling back later complete bytes, a new store seeds both boundaries from the retained prefix and reports no inherited latch, and the turn, reply, successful settlement, and otherwise valid release still complete.
- Headless and managed sessions write every observed record through the same private implementation the facade wraps without changing their stdout, stderr, reply, settlement, or attachment behavior.
- The locator matrix selects one directory before any record, resolves dot-relative and bare-relative filesystem paths against the primary config, keeps an injected store authoritative, rejects an unusable locator without host work, retains the exact prior default when unset, reuses the existing managed descriptor, and persists no `sessions` member.
- The repository facade test proves one private implementation and validator definition, both public-surface maps pin its exact names, a local strict consumer resolves its self-contained declaration and cannot name the raw manifest shape, and the packed external consumer type-checks with `skipLibCheck: false` and executes the summary plus readable/durable checkpoint contract against only the candidate package, while `spex lint`, SPDX, links, focused suites, build, package surface, release smoke, and the full suite with only the six named pre-existing failures meet their recorded gates.
