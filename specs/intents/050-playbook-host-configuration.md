<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# IR-050: Playbook host configuration

## Status

Incomplete

## Intent

Evolve the shared Playbook host configuration in three independently reviewable slices: adapter-scoped fast-mode tuning, coordinated automatic relocation of the irreplaceable user-authored config, and a proven starter agent lineup.
Keep authored SDLC workflow sources and their compiled artifacts outside this intent.

## Deliverables

- [ ] Fast mode is a Spex-coordinated, Cligent-validated optional-boolean tuning dimension whose absent, explicit-false, and explicit-true states survive complete calls and durable execution, remain nonstructural, refresh on ordinary reopen, and persist exactly on uncertain retry.
- [ ] Playbook and Spex resolve one user config below `SPEX_HOME`, with coordinated namespace ownership, target-preserving absolute locator rewrites, a path-independent durable locator grammar, retirement of the legacy regular file behind a permanent compatibility alias, unambiguous alias repair, interrupted-publication recovery, profiles-content migration, and fallback seeding specified and verified before relocation is enabled.
- [ ] The bundled Playbook starter, Spex's seed of the shared file, and public configuration documentation carry the requested Captain, Coder, and Reviewer lineup without changing notifications, enabled playbooks, or role bindings.
- [ ] Decision records, package behavior, integration verification, dependency metadata, and the full configured gate agree with the implementation without modifying excluded SDLC sources or compiled artifacts.

## Tasks

1. **Specify fast-mode semantics.**
   One commit: amend every affected tuning and continuation decision, including [DR-015](../decisions/015-per-run-agent-tuning.md), [DR-021](../decisions/021-inline-agent-settings.md), [DR-027](../decisions/027-runtime-compatibility-from-cligent.md), [DR-032](../decisions/032-explicit-roles-session-players.md), and [DR-038](../decisions/038-universal-run-resumption.md), plus the relevant spec-map summaries and `playbook-cli`, `playbook-captain`, and `release` Behavior and Verification items; define top-level and role `fastMode` as a plain optional boolean with no tagged or role-level provider-default sentinel, top-level omission as provider default, role omission as inheritance, and `false` as a retained literal request and an adapter-capability request whenever present; delegate adapter capability validation to the installed Cligent contract without a Playbook-owned support list; keep execution-projection schema `2` by adding only an optional own property whose absence remains canonical provider default; erase fast mode with model and effort when reproducing structure; exclude it from retained-generation envelope comparison; and specify current-config refresh on ordinary reopen, exact attempted-state reuse on uncertain retry, and the exact complete-call key surface before implementation.
2. **Specify config relocation.**
   One commit: add DR-043 and its decision-map row, build on [DR-021](../decisions/021-inline-agent-settings.md)'s user-config migration precedent, record Spex's live dependency on the former shared path, and amend the affected `playbook-cli` and `release` Behavior items with matching integration or system Verification items.
   DR-043 shall state that the owner directed the canonical path below `SPEX_HOME`, compare that constraint with the lower-complexity XDG-canonical inverse, disclose that the inverse avoids migration, permanent-alias reconciliation, locator rewriting, and the legacy-reload caveat, and record the accepted direction and costs without claiming that the comparison independently selected it.
   The package changes shall preserve or create separate Behavior and matching Verification items for these independently changeable concerns under [[meta-29](../meta.md#meta-29)]:

   | Concern | Contract |
   | --- | --- |
   | Shared root resolution and seeding | Resolve the first nonempty `SPEX_HOME`, otherwise the first nonempty `HOME` plus `.spex`, otherwise `os.homedir()` plus `.spex`, and retain no-overwrite starter seeding with the bundled template's copied permission mode. |
   | Namespace and lease coordination | Reserve the singular `playbook/` config namespace; leave Spex's plural `playbooks/` library and other owned siblings untouched; keep the root lease nonexclusive over the shared config; and retain the independently configurable sessions default under XDG state. |
   | Publication and compatibility alias | Publish one canonical regular file atomically without clobbering; preserve an existing canonical file's permission bits or a relocated source's exact permission bits rather than introduce a privacy cutover; carry durable transaction and completed-alias-receipt evidence; install the exact permanent legacy alias; direct compatible hosts and current documentation to the canonical pathname; bind legacy compatibility to in-place writes; and state that the alias unifies neither parent-directory watches nor pathname-derived backups. |
   | Locator portability | Rewrite only legacy-primary relative `sessions` and filesystem `from` scalar tokens to their prior absolute targets; retain every other byte and path-independent form; reject a consumed canonical-primary relative locator before an overlay can shadow it while naming its canonical replacement; preserve selected-only ordinary reopen; retain overlay-relative canonical anchoring; and bound the version-skew guarantee because an already-running legacy host can consume a new relative value before conforming validation. |
   | Migration pipeline | Order relocation or canonical seeding before verified alias installation and the retained profiles migration; keep the location, seed, and profiles notices distinct; derive current backups from the canonical pathname; and acknowledge that legacy Spex backups remain beside its lexical path. |

   The publication-and-alias Behavior and Verification pair shall express one cohesive operation through this case matrix:

   Before evaluating the steady-state matrix, the operation shall durably publish an in-flight record before its first mutation of either config entry and record the phase plus admitted before-and-after path type, bytes, and permission-mode observations.
   A matching before-state shall resume only the recorded phase, a matching after-state shall advance without replay, and any other observation shall authorize no transaction write and shall be reclassified under the matrix while retaining evidence; every matrix row therefore has an implicit precondition that no resumable in-flight transaction exists.
   A successful invocation shall emit its row's success notice only after verifying the filesystem postcondition and completion evidence, transaction recovery shall emit only the originating row's notice, reclassification shall use the destination row's stderr result, and the later profiles migration shall retain its independent notice.

   | Legacy entry | Canonical entry | Evidence | Required outcome before host work | Required stderr result |
   | --- | --- | --- | --- | --- |
   | Absent | Absent | No config bytes exist | Publish the starter at the canonical pathname without clobbering and with the bundled template's copied mode, install the exact alias, and record completion evidence. | Emit only the ordinary seed notice naming the canonical path, with no separate alias notice. |
   | Absent | Regular file | The canonical entry's bytes and permission mode remain unchanged through installation | Preserve it, install the exact alias, and record completion evidence. | Emit exactly one alias-installation notice naming the legacy and canonical paths. |
   | Regular file | Absent | The legacy source's path type, bytes, and permission mode remain unchanged through publication | Preserve its targets, unrelated bytes, and exact permission mode under the locator contract, publish canonically without clobbering, replace only the revalidated source with the exact alias, and record completion evidence. | Emit exactly one relocation notice naming both paths, with no separate alias notice. |
   | Exact alias | Regular file | The alias resolves to the canonical pathname | Use the one file unchanged and create or refresh completion evidence. | Emit no notice. |
   | Exact dangling alias | Absent | `lstat` and `readlink` prove the expected canonical target | Publish the starter directly at the canonical pathname without clobbering and with the bundled template's copied mode, retain and reverify the now-live alias, and refresh completion evidence. | Emit only the ordinary seed notice naming the canonical path, with no alias or relocation notice. |
   | Receipt-proven replacement regular file | Byte- and permission-mode-equal regular file | The completed receipt names both paths | Atomically reinstall the exact alias and refresh completion evidence. | Emit exactly one alias-repair notice naming both paths. |
   | Wrong-target alias, whether live or dangling | Any | The alias does not resolve to the canonical pathname | Reject unchanged and give manual-recovery guidance. | Emit no success notice and one diagnostic naming the legacy path, observed target, and canonical path. |
   | Regular file | Regular file | No resumable in-flight transaction exists, and the completed receipt is absent or invalid or the bytes or permission modes differ | Reject unchanged, preserve both files, and give manual-recovery guidance without timestamp ranking or canonical overwrite. | Emit no success notice and one diagnostic naming both paths, their preservation, and manual reconciliation. |
   | Unexpected filesystem type or failed publication or alias installation | Any | Safe completion is not proven | Reject before host work while preserving every recoverable regular-file byte and resumable evidence. | Emit no success notice and one diagnostic naming the failing entry or operation and whether durable evidence remains resumable. |
3. **Specify the starter lineup.**
   One commit: update the starter-lineup Behavior and Verification items in `playbook-cli` so the exact shared seed is Captain `claude` / `claude-opus-5` / `high` / auto, `dev.coder` `codex` / `gpt-5.6-sol` / `ultra` / `fastMode: true` / auto with `writablePaths: ['.git']`, and `dev.reviewer` `claude` / `claude-opus-5` / `xhigh` / auto, while notifications, all three enabled playbooks, and every role binding remain unchanged; defer both hosts' templates, public documentation, and executable assertions until the compatibility and implementation tasks.
4. **Gate fast mode and the lineup on Spex compatibility.**
   One commit: after separately decomposed Spex work has adopted the fast-mode and starter-lineup specifications, record its accepted decision, available compatible app release, integration evidence, and full-gate evidence in Playbook's decision, release, and configuration contracts; block Playbook fast-mode implementation, public grammar documentation, package release, and starter implementation until that app accepts both booleans in Captain, player, and role positions, delegates adapter capability validation, projects the value into calls, preserves it through every Settings edit, and seeds the exact shared lineup of Task 3; and state that the compatible app release is a development prerequisite and a user-upgrade prerequisite before authoring fast mode rather than a detectable installed dependency.
5. **Implement and verify fast mode.**
   One commit: after Task 4 is satisfied, raise `@sublang/cligent` to `^0.24.0` with its lockfile update; thread optional `fastMode` through normalized Captain and player defaults, role overrides, `SessionAgent`, Captain and player call construction, published runtime and declaration surfaces, launch-config resolution helpers, execution projections, structural erasure, and durable optional-key allowlists while preserving explicit `false`; use Cligent's public capability assertion for every present `fastMode` value, including `false`, before registry preparation, import, readiness, or host work; update configuration and CLI documentation, audit README, correct the starter-template grammar commentary, and identify the required Spex app upgrade; and add focused integration, legacy-schema-2 absence, explicit-boolean, reopen, retry, retained-generation, dependency-surface, and installed-Cligent capability coverage, running the affected capability suite independently of the known full-suite blocker.
6. **Implement the starter lineup.**
   One commit: after Tasks 4 and 5 are satisfied, update the bundled template, configuration documentation, and exact seed/template integration assertions to the shared lineup specified by Task 3 while leaving notifications, all three enabled playbooks, and every role binding unchanged.
7. **Gate relocation on Spex compatibility.**
   One commit: after separately decomposed Spex work has adopted DR-043, record its accepted decision, available compatible app release, desktop and server integration evidence, and full-gate evidence in Playbook's decision, release, and configuration contracts; block Playbook relocation until both Spex shells apply Task 2's exact root fallback, directly resolve, seed, watch, edit, and back up the canonical path without replacing the compatibility alias, reserve the singular namespace outside the exclusive file-state lease and migrations, enforce the path-independent primary-locator grammar, and adopt the same migration and publication-state matrix; and state that the app release is a development prerequisite rather than a detectable installed dependency, that the permanent legacy alias carries installed runtime path skew only for legacy in-place writers, and that an already-running legacy Spex may require reload after a direct canonical-path edit.
8. **Implement config relocation.**
   One commit: after Task 7 is satisfied, implement the shared `${SPEX_HOME:-$HOME/.spex}/playbook/playbook.config.yaml` resolver and interrupt-safe relocation before either front end reads config-dependent state; retain help and raw-config no-write boundaries; implement every Task 2 transaction boundary and state-matrix outcome, including pre-mutation durable evidence, before-state resume, after-state advance without replay, changed-state reclassification, direct canonical seeding behind an exact dangling alias, refusal of a wrong-target live or dangling alias, legacy-reader in-place read and write-through, legacy atomic-replace conflict refusal, old-host no-reseed behavior, one underlying regular file, exact `0600` and `0644` source-mode retention, bundled-template seed mode, and every specified stderr and backup result; cover a byte-identical destination when no locator changes, exact bytes outside rewritten scalar tokens otherwise, identical absolute targets through both pathnames, canonical-primary relative rejection even when shadowed with an absolute replacement, accepted path-independent forms, selected-only ordinary reopen, the overlay-anchor cutover, failed-rewrite no-publication, profiles composition, and idempotence through focused integration tests; update README and configuration documentation to direct edits to the canonical path and explain the permanent alias, path-independent primary grammar, pathname-derived backup, unsupported alias replacement, manual conflict recovery, and legacy-reload rules; and update normal CLI helpers plus packed release-smoke, live-acceptance, and CI isolation harnesses to seed and select an isolated `SPEX_HOME` without masking real adapter authentication or accidentally exercising the legacy migration path.
9. **Run the final gate and close the intent.**
   One commit: after the preceding inputs settle, run `pnpm test` once, require no new failure outside the six known source-to-artifact cases — three in `src/slc-source-contract.test.ts`, two in `reference/sdlc/code.playbook/code.gears-fsm.test.ts`, and one in `reference/sdlc/decide.playbook/decide.gears-fsm.test.ts` — record which of those cases still exist and the exact gate evidence here, check every deliverable, mark this intent done, and leave the excluded authored SDLC sources and compiled artifacts untouched rather than repairing their separate drift.

## Verification

- Config normalization, interactive composition, headless execution, Captain and delegated-player calls, durable reopen, and uncertain retry preserve absent, explicit-false, and explicit-true fast mode through the installed Cligent surface and reject both present booleans for an unsupported adapter through Cligent before registry preparation, import, readiness, or host work.
- Existing execution-projection schema-2 data without fast mode remains canonical and reopenable, while explicit booleans survive execution projections and exact retry, structural projection erases fast mode, and retained-generation compatibility ignores it.
- Before Playbook implements or documents fast mode or implements the proven lineup, compatible Spex integration evidence proves complete fast-mode acceptance and round-trip plus exact shared-seed parity.
- Before Playbook enables relocation, compatible Spex desktop and server integration evidence proves both shells apply the exact root fallback, choose the same canonical path, and preserve the reserved namespace, locator grammar, and complete publication-state matrix.
- Every Task 2 transaction boundary and publication-and-alias matrix row proves that config bootstrap records recovery evidence before mutation, resumes a matching before-state, advances a matching after-state without replay, reclassifies a changed state without a transaction-authorized write, publishes at most one no-clobber canonical regular file with the bundled-template seed mode or exact relocated source mode, recovers an exact dangling alias by seeding its canonical target directly, rejects a wrong-target alias unchanged, keeps a healthy alias reachable for legacy in-place reads and writes, repairs only a receipt-proven byte- and permission-mode-equal replacement, prevents old-host reseeding while the alias is intact, detects an alias-breaking split before Playbook host work, and preserves every recoverable user byte and exact required stderr result.
- Relative-locator, overlay-relative, malformed-rewrite, and retired-profiles inputs prove relocation preserves every migrated primary-file locator's absolute target through either pathname, rejects a later consumed primary-relative locator with its canonical absolute replacement even when an overlay shadows it, preserves selected-only ordinary reopen and unrelated bytes, distinguishes canonical and legacy pathname-derived backups, states the overlay, unsupported-replacement, manual-recovery, and legacy-live-reload cutovers, and orders relocation or seeding before alias installation and the existing in-place content migration.
- Parsing the bundled and newly seeded starter configs in both hosts proves the exact requested agent blocks while notifications, enabled playbook modules, and role maps remain unchanged.
- `spex lint` reports no errors after every spec edit, and the final `pnpm test` run reports no failure outside the six enumerated pre-existing source-to-artifact cases.
