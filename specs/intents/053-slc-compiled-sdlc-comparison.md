<!-- SPDX-License-Identifier: Apache-2.0 -->
<!-- SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai> -->

# IR-053: Compile the SDLC Workflows Through `slc playbook`

## Status

Comparison complete (2026-09-03); commit hashes cited below predate the 2026-09-04 history rewrite that corrected every co-author trailer to git-4, so resolve them by subject. all four sources compiled through reviewed `slc playbook` at least to an accepted FSM, three to a finished bundle; verdicts in task 2, improvements in task 3. Chronology of the attempts follows. The Codex quota reset; all four reviewed compiles relaunched on slc 0.7.0 with `SLC_STALL_TIMEOUT=2400`; attempt 3 lost all four at ~20 min to the Claude session limit hitting the Opus reviewer (the compiler failed closed correctly); attempt 4 launched after the reset.
   - Finding: the Codex coder reads beyond what the definition cites — it located the shipped canonical artifacts inside `node_modules/@sublang/playbook/reference/sdlc/*.playbook/` and copied one byte-for-byte, so attempts 1–4 are contaminated; attempt 5 runs in workspaces outside the SubLang tree, each with its own `node_modules` holding a stripped copy of the engine passed as the explicit `--link` target (slc's own install stays pristine so the pins remain current), and the harness flags any byte-identical output as tainted.
   - Finding (confirmed defect, fixed in slc): the coder's final message was a valid correction envelope, but cligent 0.23+ joins Codex's progress commentary in front of the final message in `finalText`, and slc's parser accepted only bare JSON or one lone fence — so every narrating Codex coder failed the reviewed loop closed after ~30 min. slc now takes the last complete top-level JSON object in the reply as the envelope (spec-first under DR-022; slc `ce1fe11`). Residual: the reviewer's text verdict (`NO_FINDINGS` / `FINDINGS:`) has the same exposure for a narrating Codex reviewer; a separate decision if a Codex reviewer is ever configured. Attempt 6: the reviewed loop completed for `code` (the envelope fix works), then the compiled phase's hidden judge call failed — slc passes `allowedTools` to control calls and the Codex adapter cannot enforce tool lists; playbook's DR-013 A1 prompt-level fallback was never applied in slc, unseen until a Codex agent ran a compiled phase. Fixed in slc (`d5a5d4c`, phase-execution-31/-32); attempt 7 launched. Attempt 7 lost all four to the Claude session limit (four concurrent ultracode reviewers), after `decide` had an accepted text2gears output — which the pipeline discards because snapshots publish only on full-run success (DR-021). Design finding: expensive multi-phase compiles need per-phase checkpointing of accepted outputs; salvaged `decide` through phase-wise runs and throttled to two compiles at a time.

## Intent

The maintained artifacts were recompiled by agents following the definitions directly; compile each SDLC source through `slc playbook` — reviewed two-agent compilation with a Codex coder in fast mode and a Claude reviewer — and judge whether the compiler's output is equivalent, better, or flawed, replacing an artifact only on solid real-run evidence and improving the compiler or definitions only where a flaw is essential and general.

## Deliverables

- [x] Each of `code.md`, `review.md`, `decide.md`, and `dev.md` compiled through reviewed `slc playbook` into a scratch location (`review` to an accepted FSM whose link the linker rejected).
- [x] A comparison per workflow: conformance suites, prompt contracts, and real runs, with a verdict of equivalent, better, or flawed (no real run of a compiled bundle: none earned it).
- [x] Flaws traced to their cause with a minimal general improvement or an explicit decision to leave them.

## Tasks

1. [x] Compile the four sources through reviewed `slc playbook` and collect the artifacts: launched 2026-09-02 with slc main (db83c66, Playbook 12.1.0 adopted), coder `codex` / `gpt-5.6-sol` / `ultra` / fast mode, reviewer `claude-code` / `claude-opus-5` / `ultracode`. The first `dev` attempt failed closed at text2gears after 22 min: the reviewer call showed no activity for the default 600 s stall budget; relaunched with `SLC_STALL_TIMEOUT=2400`; `decide` (38 min) and `review` (42 min) failed the same way, so all four were relaunched with the 2400 s budget; the relaunches then failed on the Codex usage limit (`review`, `code`, `decide`), and `dev` was stopped. No compile reached gears2fsm; no artifact to compare yet. Finding for task 3: an `ultracode` reviewer can stay silent beyond the default 600 s stall budget, so reviewed compilation needs either a documented larger default for deep reviewers or activity signals from the adapter.
2. [x] Compare each against the maintained artifact and record verdicts here — `code` equivalent, `decide` flawed, `review` flawed, `dev` flawed; no replacement earned:
   - `decide` raw GEARS (attempt 7, 45 min through the reviewed loop): not contaminated, four items, source-fidelity clean; the `proposed` affirmative-completeness contract is declared only on the Reviewer's proposal, not the Coder's, which the maintained artifact declares on both — pending the FSM to judge whether the parallel join still gates both. Its optimize pass (attempt 8, phase-wise, 7 min) changed nothing, matching the maintained artifact's absence of script actors. Its gears2fsm (3 h 03 min) failed closed at the third review call — verdict for this phase: **flawed, and correctly rejected**. The Opus reviewer found unguarded catch-all `BOSS_REPLY` arms on the parallel parent and the root, so a stale or unknown reply id exits the whole proposal round to `failed` and discards the sibling branch's pending question, against the definition's branch-local suspension rule; slc's independent verifier confirms the findings unique to the compiled FSM (`an unknown BOSS_REPLY questionId moved the branch` at both branch waits, `proposed` and `committed` with no reachable accepting transition) while the maintained FSM has none of them. Calibration: that verifier is not an oracle — it also fails the maintained `decide` (15 findings, mostly missing branch-node ids and nested-arm probing limits) and `code` (3) bundles, so only compiled-only findings count. The coder reported "exact GEARS–FSM conformance with no findings" on the rejected artifact, so its self-verification claim was false. A phase-wise gears2fsm retry (attempt 11) was refused by the Claude weekly limit at 13 min; the retry on credit (attempt 13, 53 min) was accepted: the maintained FSM's eight top-level states, one parallel group, four finals, and `BOSS_REPLY` arms only on the three wait leaves, each guarded — no unknown-id finding remains, so the branch-local discipline converged on a fresh attempt. What remains compiled-only in the verifier is a naming flaw: the GEARS declares quoted kebab-case output properties (`reviewer-proposal`, `coder-output`, `decide-commit`) and the FSM types and reads them so, which the verifier's identifier-pattern field parser cannot synthesize and which breaks `dev`, whose maintained FSM consumes `decide`'s terminal `decideCommit` by name. The Source names no fields, and the definition asked only for an "exact case-sensitive identifier" — a second definition gap, closed in `slc/text2gears.md` (output properties match the guard-name identifier pattern). The link (3 h 13 min) was accepted and the bundle finished: source fidelity clean, the maintained artifact's eight states and four finals, a 5.8 k-line bespoke parallel runtime (the maintained is 5.1 k), and the emitted prompt-contract suite fails with `commitCoderProposal: composePlayerPrompt threw … used role "reviewer" instead of canonical local role "coder"` — **a checker false positive, not a linker defect**: the maintained `decide` and `review` bundles fail the same check at their Coder states because slc's lookup rejects any role but the acting one while the link definition rejects only an undeclared role (a Coder prompt names the Reviewer through `<reviewer-llm>`), and the maintained `dev` fails it because an array-typed context field is synthesized as a string; the check is being fixed in slc and DR-030's gate must not go live before it; the emitted coverage suite repeats the verifier's kebab-key findings; DECIDE-1 declares no `proposed` contract, so the Coder's proposal branch completes on any output where the maintained requires an affirmative proposal. Maintained-suite run skipped: the identifier flaw alone decides it. **Final verdict: flawed** — naming (definition gap fixed) and a weakened Coder-proposal contract; the composer finding is withdrawn as a checker false positive. Validation of the identifier rule under review (a fresh text2gears run on the fixed definition, 28 min): the coder declares `coderOutput`, `decideCommit`, and `reviewerProposal`, no kebab-case key remains, fidelity clean, not contaminated; DECIDE-1 still carries no `proposed` contract, which stays a Reviewer concern. Under the corrected checker the finished bundle's composer throws on an ordinary turn at all three player states (`Cannot read properties of null (reading 'replaceAll')`): it quotes relayed values without guarding the `null` the FSM initializes them to, which the maintained composers guard — a real first-turn fragility, unlike the withdrawn role finding. Reviewer contamination note: the reviewer read the maintained reference under `playbook/reference/sdlc/` to prescribe the fix; that taints the review's independence, not the artifact.
   - `code` raw GEARS (attempt 8, 46 min through the reviewed loop): not contaminated, four items with the maintained IDs, source-fidelity clean; optimize changed nothing (matching the maintained artifact); gears2fsm accepted after 1 h 35 min with the maintained artifact's nine top-level states and two finals. Its link (43 min) failed closed on a **malformed verdict**: the Opus reviewer prefaced `NO_FINDINGS` with its verification rationale, which slc's verdict parser rejected — the narration exposure noted above, now hit by a Claude reviewer on an approved artifact. Fixed in slc (`0ba8213`, DR-022 and phase-execution-46/-47): the verdict is read from the end of the reply, symmetric to the envelope rule, and the Reviewer prompt confines inspection to the request's workspace. Second finding in the same verdict: the reviewer compared the linker's output against the maintained artifact under `playbook/reference/sdlc/` and the coder's fix converged to "byte-identical to the reference" lines — so the reviewer's own workspace-external reading contaminates the linked module through guidance; the slc fix also confines Reviewer inspection to the request's workspace. The accepted `code.fsm.ts` stands; the link phase will be rerun phase-wise after the fix. GEARS judgment: semantically equivalent to the maintained artifact and slightly richer — it inlines the source's stated preconditions as `Where`/`When` clauses (review ownership, receipt-authoritative commit identity), carries the caller-input relay in quotes on both Coder prompts, and extends every result description with the source's "affirmatively established … selection shall not depend on a fixed presentation format" language, which reaches the judge contract as useful instruction; it drops the optional `## Coder` heading and shortens the title, both permitted by the definition. Maintained-suite evidence (compiled GEARS, FSM, and the reviewer-approved-then-rejected linked module swapped into a playbook worktree at `scratch/slc-compare`, six artifact suites): 46 pass, 8 fail, all 8 passing in the control — every failure is a pin of the maintained implementation, not a behavioral regression: (a) an extra blank quoted line between the relayed blocks of the IR-task REVIEW input (source says "additionally input", prescribing no separator — cosmetic); (b) `awaitBossReply` arms named `canResume` with the catch-all `#failed` last instead of `emptyBossReply` first plus per-state resume guards — same acceptance of an omitted id with one pending question and same empty-answer failure, differing only on a mismatched id (compiled routes to `failed` per the definition's malformed-reply rule, maintained stays); (c) the failed-REVIEW diagnostic is generic text instead of `REVIEW returned an invalid approval result: <json>` (presentation, loses the offending payload); (d) the gears-fsm suite pins the maintained outcome bullets, which dropped the source's "start no further phase" and "instead of reporting an authored review outcome" clauses that the compiled GEARS keeps. Compiled outside the repo, the linked module imports `@sublang/playbook/playbook-captain` rather than `./playbook-captain.js`, and its `CodingContext` makes every field required, which breaks only the hand-written `code.fsm.introspect.ts` sidecar's partial contexts and the in-repo `tsc` build — consequences of the compile location and of a typing choice, not of the workflow's behavior. **Verdict: equivalent** — a sound artifact, better on GEARS fidelity, not better enough to replace the maintained one (no real-run evidence of improvement, and replacement would re-pin six suites and the sidecar); The phase-wise link rerun on the fixed slc (40 min) accepted; with the workspace-confined reviewer the coder imports `./code.fsm.ts` by its `.ts` extension and hand-writes a structural `HostCapabilities` interface with `unknown`-typed methods instead of deriving it from the engine's exported type — a typing-quality step down from the reference-steered version, invisible at run time. Full harness on the finished bundle: source fidelity clean, GEARS item/result structure and the FSM's nine states and two finals identical to the maintained artifact, slc's independent verifier **PASS with no findings** (the maintained bundle carries three), and four of six emitted suites green — the two failures assert "unsupported link-target provenance `@sublang/playbook@12.2.0`", a provenance sidecar my deterministic finisher does not write, so harness-side. Correction: the provenance assertion fires only because my finisher passes the link-target provenance without the engine declaration; slc's real full-link path reads the declaration when its version table misses and supplies the schema-3 witness (its own demo references at 12.2.0 carry no such assertion) — harness-side after all. Maintained-suite rerun on the confined linked module: 44 pass / 10 fail — the seven FSM/GEARS pins unchanged, no failure gone, and three new ones from one defect: the composer emits a stray empty quoted line (`> `) where an omitted optional run-results relay was, in both Coder prompts and the REVIEW input; the reference-steered attempt-8 reviewer had caught exactly this, the workspace-confined one did not. The `.ts` sibling specifier is permitted by the link definition only for a source-only host, which the coder inferred from the package-less scratch workspace; inside the repository it fails `tsc` (TS5097) and the emitted `.js` would not resolve — a compile-location consequence, not a compiler flaw. **Link verdict: equivalent with one minor prompt defect; overall `code` verdict stands: equivalent, not a replacement.**
   - `review` raw GEARS (attempt 8, 47 min through the reviewed loop): **flawed** — five items where the source has four; the invented `REVIEW-0` turns the source's Captain rule "take the evaluated repository revision from repository authority, not from either player's prose" into a Captain prompt item with two non-authored prompt lines and an `obtained` result, although that revision is an effect-owned field the host injects from the repository receipt. The Opus reviewer accepted it; the deterministic source-fidelity checker rejected it at once. Run killed after text2gears (output kept as `review.playbook.attempt8`), a second sample launched. Second sample (attempt 10, 57 min): no invented item — four items with the maintained IDs and identical result contracts, source-fidelity clean, not contaminated; it folds the source's invariants (baseline scope, committed work only, receipt-authoritative commit identity) into long "under the invariants that …" condition clauses on every item where the maintained GEARS keeps them as preamble prose, and names the `rejectedAll` arm's injected revision `repositoryRevision` instead of the maintained `evaluatedRevision`. So the REVIEW-0 invention was a one-off coder deviation the reviewer missed, not a systematic misreading; optimize changed nothing. Its gears2fsm reviewed loop was lost at 1 h 28 min to the Claude weekly limit on the reviewer, after the coder had written a complete FSM it claimed conformant (kept as `review.fsm.attempt10.unreviewed.ts`); rerun phase-wise on credit: gears2fsm accepted (46 min) with the maintained FSM's eight states and one final, and one compiled-only verifier finding, `reviewInitial has no reachable entry transition`, which is real: the GEARS decomposes the caller's input into five typed relay placeholders (a compiler judgment the Source permits) but also relays `> <repositoryRevision>` into every Reviewer prompt, a value the Source never relays, and the FSM takes it from machine input and gates `START_REVIEW` on its being non-empty — an invented entry contract. The fidelity checker exempts bare placeholder lines, so it passed; the Reviewer accepted it. **Verdict: flawed** (invented relay and entry contract); the link phase then ended in the linker's `rejected` outcome after 1 h without writing a module — the verifier's entry finding was the earlier, cheaper signal.
   - `dev` raw GEARS (attempt 10, 30 min through the reviewed loop): not contaminated, source-fidelity clean, four items with the maintained IDs and an identical DEV-1 result contract; the three nested-call items carry `succeeded`/`notSucceeded` `Results:` lists and fold the source's failure-parking and commit-identity rules into `Where` clauses, where the maintained GEARS keeps nested outcomes as prose — the same compiler kept nested outcomes as prose in its own `code` output, so this is run-to-run representation variance; optimize changed nothing (matching the maintained artifact). gears2fsm (1 h 04 min) accepted with the maintained FSM's ten states and three finals, but slc's verifier rejects the GEARS with "nested-playbook call item shall not declare Results metadata" on DEV-2/3/4 — a rule stated only in slc's verifier, absent from `text2gears.md`, whose "move Source's outcome contract into `Results:`" carries no nested-call exception; two reviewed loops accepted the letter-of-the-definition output. **Verdict so far: flawed by a definition gap**, fixed in `slc/text2gears.md` (`6fe4c84`); its link phase then ended in the linker's `rejected` outcome without writing a module. Recompiled from the fixed definition (a patched pipeline copy inside slc's tree with regenerated pins): the coder now keeps nested-call outcomes as prose, citing the new rule (that unreviewed GEARS, kept as `dev.gears.raw.attempt13.md`, passes the fidelity checker and raises no verifier finding), but the reviewer call died on a transient `529 Overloaded` and slc failed the phase closed, discarding the finished coder work — fixed in slc (one retry of an errored non-stall Reviewer call before failing closed, DR-022). Recompile from the fixed definition (attempt 15, 23 min through the reviewed loop): accepted, `Results:` only on DEV-1, fidelity clean, no verifier finding, not contaminated — the definition fix is confirmed under review. Its gears2fsm then ended in the transform bundle's `rejected` terminal after 1 h 18 min on a process predating the rejection diagnostic, reason unrecoverable; gears2fsm rerun phase-wise on the current slc: accepted (57 min) with the maintained FSM's ten states and three finals and **zero verifier findings** (the maintained bundle carries eighteen, its input composition throwing under introspection). GEARS judgment: semantically equivalent and slightly more explicit than the maintained artifact — the "Workflow outcomes" prose is folded into `Where` conditions and result descriptions, the `decide` success proof names `decideCommit` and `evaluatedRevision` explicitly (identifiers, unlike the compiled `decide`), placeholders are camel-case, and the title is shortened. Maintained FSM-level suites in the worktree (gears-fsm, coverage, introspect): 13 pass / 9 fail, all worktree-only, in four groups: (a) **a real semantic defect** — `callCode.onDone` accepts any JSON-safe child output as success, so a CODE terminal result that does not prove success reaches `done` instead of the authored `reportedChildFailure`, and the two `completeWithInsufficientCodeResult` terminal meanings are missing; the maintained guard checks CODE's `status: complete`, `lastCodeCommit`, and `allReviewsPassed`, knowledge the Source never states (`dev.md` says only "a terminal result that does not prove the success required for the selected path") and the compiler cannot have — a **Source gap** to put to the human owner of `dev.md`, since the compiler encodes only what the Source says; (b) the stray empty `> ` line in composed child-call text, the same defect family as the `code` relink; (c) the introspect sidecar's partial contexts leave the child-call template unsubstituted because the compiled FSM composes `childCallText` in an action the sidecar never runs — a sidecar pin, not a runtime defect (confirmed: the action composes the quoted relay from the accepted planning values); (d) lawful differences the suites pin — JSON-safe `lastError` records as the definition requires where the maintained stores `Error` instances, and reworded outcome bullets. **Verdict: flawed by (a), traced to the Source, not the compiler.** The link (1 h 08 min) was accepted and the finished bundle is the cleanest of the four: six of eight emitted tests green (the two failures are the harness-side provenance assertion), slc's independent verifier **PASS with no findings including the linked module**, source fidelity clean, and item, result, state, and final structure identical to the maintained artifact. Maintained suites with the linked module: 26 pass / 15 fail (control 41/0); beyond the nine FSM-level failures, the six new ones are the stray `> ` line in composed Analyst prompts and child-call text (three tests; the same family as the `code` relink, now traced to a link-definition gap and closed in `slc/link.md` and `slc/gears2fsm.md`), the terminal envelope publishing `status: complete` with `childOutput`/`childPlaybookId` where the maintained publishes `child-failed` with `childResult` for a CODE result that proves nothing (the same Source gap as (a), plus field names nothing consumes), and **one real functional miss**: the linked module never populates the discussion-context relay after a Boss reply (`Received: undefined`), which also hangs the suspend-and-resume test — the Source's "relevant discussion context" is machine-tracked in the maintained artifact and simply absent here, and the Reviewer did not notice. The `.ts` sibling specifier recurs (compile-location consequence). **Final verdict: flawed** — one Source gap (success proof), one definition gap (empty relay, fixed), one reviewer miss (discussion context). Under the corrected checker (declared-role lookups, typed initial context, introspection under initial context) the bundle still has no compiled-only verifier finding: its three "nested-call text not the GEARS prompt verbatim" lines are shared with the maintained bundle, whose child-call text is likewise composed at run time.
3. [x] Land any essential improvement with its decision record, or record why none is warranted — everything general that a real failure justified is landed in slc (unreleased after 0.7.0) or in this repository's definitions (unreleased after 12.2.0); left open by decision: the FSM coverage verifier stays a post-link diagnostic until its probing noise on valid artifacts is removed, and completed-prefix salvage stays rejected per DR-021. Also landed with the DR-030 calibration: the prompt-contract check's own false positives (slc `a1b51ba`: declared-role identity lookups accepted, ordinary turns synthesized from the machine's initial context, introspection under that context) and the identifier-pattern rule in the DR-029 gate (`b3f2ddd`). Landed: verdict read from the end of the reply and Reviewer confined to the workspace (slc `0ba8213`); the nested-call `Results:` rule stated in `slc/text2gears.md` (playbook `6fe4c84`); one retry of an errored Reviewer call before failing closed, structurally excluding stall aborts (slc `79e64a9`, `d24cd54`). Landed in slc: DR-029 (deterministic Source-fidelity gate after text2gears, `0d91432`: verification-25/26, phase-execution-51/52, `src/verify-source.ts`, findings relayed to the Coder in place of a Reviewer call on both the interpreted and the compiled path (`2761822`, phase-execution-25/51/52), with a post-result fail-closed backstop; validated on the real samples: two findings on the attempt-8 `review` GEARS with the invented item, none on the clean attempt-10 GEARS) and the DR-021 amendment (`fcd13ff`: a failed run names its accepted phases and the single-phase invocation that resumes from the last one; completed-prefix salvage stays rejected as DR-021 intends). Candidates still open:
   - Per-phase checkpointing of accepted outputs, so a later-phase failure does not discard hours of accepted work (today only a full-run success publishes a snapshot).
   - Landed as slc DR-030 (`f1e76c0`, phase-execution-53/54, verification-27/28, `checkLinkedModuleContract`) — mechanical pre-review verification for the link phase, the DR-029 pattern applied to the linker; its calibration exposed that the prompt-contract check itself had false positives on three maintained bundles (declared-role identity lookups rejected; array-typed context synthesized as strings), fixed before the gate can be trusted: the emitted prompt-contract check and the linked-module import are precise (no noise on the maintained bundles) and would have relayed `decide`'s invalid composer to the Coder before the first Reviewer call instead of after a 3-hour acceptance; the FSM coverage verifier is not a candidate gate until its noise on valid artifacts (missing branch-node ids, nested-arm probing limits) is removed.
   - A deterministic source-fidelity gate after text2gears (every prompt line an authored fragment of Source, no invented items): slc runs no verification at all on text2gears output today, the reviewed loop passed an invented item, and `scripts/check-slc-source-gears.mjs` in this repository already implements the check.
   - Landed: a compiled phase that ends in an output-less authored terminal (the link bundle's `rejected`) now names the reached state and carries the performing call's final text like a failure does (slc `5a883d7`, phase-execution-24/26); two link rejections (`dev` attempt 10, `review` attempt 13) had left no recoverable reason because slc keeps no trace of a compiled phase.
   - A deep reviewer's silence exceeds the default 600 s stall budget (documented larger default or adapter activity signals).

## Verification

- Every compiled artifact passes its emitted verification and the independent review before comparison.
- Any replacement passes the maintained conformance suites and a real run before it lands.
